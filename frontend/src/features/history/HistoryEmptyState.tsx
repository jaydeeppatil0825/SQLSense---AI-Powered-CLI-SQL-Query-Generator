import { EmptyState } from "../../components/shared/EmptyState";

export function HistoryEmptyState() {
  return <EmptyState title="No query history yet" message="Ask your data to build a session history." />;
}
