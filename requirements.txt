sqlalchemy==2.0.36
pymysql==1.1.1
python-dotenv==1.0.1
requests==2.32.3
hypothesis==6.119.3
pytest==8.3.4
pandas==2.2.3
# Note: On Windows, pandas requires C build tools. If installation fails, the app will use
# rule-based insights instead (which work without pandas).
matplotlib==3.9.4
numpy==2.1.3
cryptography==43.0.0
